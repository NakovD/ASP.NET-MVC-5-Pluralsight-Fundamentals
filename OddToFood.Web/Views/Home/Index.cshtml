@model IEnumerable<OdeToFood.Data.Models.Restaurant>

<div class="jumbotron">
    <h1>@Model.Count()</h1>
    <p class="lead">some haeding</p>
    <p><a href="#" class="btn btn-primary btn-lg">whattup homie</a></p>
</div>

<ul>
    @foreach (var restaurant in Model)
    {
        <li>@restaurant.Name</li>
    }
</ul>